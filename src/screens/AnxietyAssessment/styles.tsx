import { StyleSheet } from 'react-native';

import adjust from '~Root/utils/adjust';

export default StyleSheet.create({
  resultTextWrapper: {
    marginTop: adjust(192),
    marginBottom: adjust(192),
  },
});
