import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  cardFormInput: {
    paddingHorizontal: 14,
    paddingTop: 16,
    paddingBottom: 25,
    borderRadius: 12,
    rowGap: 30,
  },
  cardTextInput: {
    paddingHorizontal: 14,
  },
});
